USE TEST
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE PROCEDURE [dbo].[SPDalumnos]
@idAlumno BIGINT 
AS
BEGIN
	DELETE FROM Alumno WHERE IdAlumno = @idAlumno
END

SELECT * FROM Alumno
EXEC [SPDalumnos] 4
